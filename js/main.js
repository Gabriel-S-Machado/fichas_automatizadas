                //Lazy.js
    //funções

    function logg(a){
        console.log(a)
    }
    
    function verObj(objeto, executar){
        Object.entries(objeto).forEach(([key, value])=>(
            executar
        ))
    }
    
    //vars

    const x = "X";
    const marca = "------------------";
    const pular = (pulos) => {let ns = ""; while(pulos !== 0){ns = ns + "\n"; pulos-- } return ns ;};

                //Codigo
    
logg("a")
                //Planejamento

    //Fazer e atualizar Fichas automaticamente.
    //Criar novas Fichas automaticamente
    //Mudar as Cores de acordo com o tipo de Ficha
    //derrepente colocar uma PNG do lado do Titulo do Site
    